name: GHActions Label Demo
run-name: Workflow on Labels - ${{ github.actor }}
on:
  label:
    types: [created, edited, deleted]
jobs:
  GHActions:
    steps:
      - run: echo "Running GH Action when a label is created"